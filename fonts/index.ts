import { Inter, Oxanium } from 'next/font/google';

const inter = Inter({
    variable: '--font-inter',
    subsets: ['latin']
});

const oxanium = Oxanium({
    variable: '--font-oxanium',
    subsets: ['latin']
});

export { inter, oxanium };
